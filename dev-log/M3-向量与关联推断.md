# M3 向量与关联推断

## 交付内容

- Chroma `notes` / `events` 双集合及持久化。
- 支持注入式确定性 embedding 与模型指纹校验。
- 代码生成文件夹、命名、时间和语义候选。
- LLM 对候选关联进行结构化判定，保留依据、证据和置信度。
- 关联结果写入 SQLite `associations`。
- 删除/缺失笔记的关联清理、失败隔离和跨 Run 幂等。

## 约束

模型指纹不一致时拒绝静默写入，避免向量相似度失效。

## 状态

- 完成（2026-09-02）
- 相关提交：`f49dc41`
