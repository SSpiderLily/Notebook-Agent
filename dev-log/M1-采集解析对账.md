# M1 采集解析对账

## 交付内容

- Vault 递归扫描 Markdown 文件。
- 解析 YAML frontmatter、中文标签和双链。
- 生成稳定 `note_id` 与内容哈希。
- 支持目录排除、glob 忽略和 `noteagent: ignore`。
- 生成变更快照，识别 missing / ignored / active 状态。
- 提供扫描试算：篇数、字符数、预计调用数、费用和耗时。

## 关键约束

原始笔记只读；采集阶段不修改 Vault 文件。

## 验证结果

M1 边界测试覆盖改名、删除、忽略、空 frontmatter、嵌套标签和孤儿对账。

## 状态

- 完成（2026-09-01）
- 相关提交：`eb8a863`、`188fff6`
