# M0-2 RunManager 运行状态机

> 内容单点：本篇是大纲里"M0-2 完成"这一条的**档案**，细节改动只在本篇进行，不回写大纲。

- **交付**：loguru 结构化日志（JSON 行、轮转、每 Run 独立日志）+ RunManager 状态机
- **技术**：SQLAlchemy，表 `runs` / `stages`；互斥控制、断点查询
- **状态**：✅ 完成（2026-09-01）
- **来源**：commit `e32bd3c`

## 决策 / 变更记录（追加式）
- [2026-09-01] 状态与日志分离设计：日志由 loguru 按 Run 独立落文件，运行状态由 SQLAlchemy 落库，二者通过 run_id 关联
  原因：日志为可观测、状态为可断点，避免耦合
  状态：完成