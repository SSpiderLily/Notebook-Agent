# M2 抽取持久化与增量

## 交付内容

- 抽取结果写入 SQLite `extractions` 表。
- 事件写入 `events` 表，保留时间线索、状态线索和笔记内顺序。
- 内容未变且已有成功抽取记录的笔记跳过 LLM。
- 抽取 StageIO 保留成功结果和失败清单。
- 失败项支持单独重试，重试成功后更新数据库和 StageIO。
- 增加 LLM 调用台账、token/费用估算与 Run 成本上限。
- 兼容真实 Provider 返回的 JSON 围栏格式。

## 验证结果

抽取持久化、增量跳过、失败恢复、重试持久化和 Provider 格式兼容测试通过。

## 状态

- 完成（2026-09-01）
- 相关提交：`3464783`、`419d652`、`7cd7a2c`、`8c1b870`、`e812108`、`bd24142`

## 归属

- 进度看板：[[进度看板]]
