# NoteAgent 需求规格说明书（SRS）v1.0

> 状态：已经过 13 轮需求讨论并逐项确认（确认记录见第八节）。
> 本文档是概要设计、详细设计、实现与测试的依据；原始需求留档见 `Plan.md`。

## 一、引言

### 1.1 目的

定义 NoteAgent（Obsidian 笔记自动整理智能体）完整需求。

### 1.2 范围

单用户**本地 Web 服务**（FastAPI 后端 + Vue 3 前端，浏览器访问）；输入为 Obsidian 仓库；输出为树重建产物、Web 可视化与问答服务。不含多用户、云端部署（架构预留）。

### 1.3 项目定位

Agent 项目，双重学习目标：

1. **Agent 开发**（学习优先）：刻意采用 ReAct / 工具调用 / 记忆等模式（LangChain 生态），允许为学习价值牺牲一定简洁性
2. **后端工程**：API 设计、异步长任务、进度推送、持久化、自动化测试

### 1.4 术语

- **Vault**：Obsidian 仓库（本质是装满 .md 文件的本地文件夹）
- **frontmatter**：笔记头部 YAML 元数据
- **树**：一个任务/想法及其全部后续推进构成的路径结构；**树根**=起点；**节点**=树上的动作/事件
- **森林**：全部树的集合
- **断头路径**：想法提出后无后续推进、未闭合的路径
- **事件抽取**：从一篇笔记中拆出多个独立动作/事件（日记式粒度）
- **草稿森林**：智能体推断生成、未经用户确认的树结构

## 二、总体描述

### 2.1 领域模型（核心）

笔记本质是**森林结构**：任务/想法 = 树（根），后续动作/事件 = 节点，事件完成则路径闭合。路径状态：**已完成 / 进行中 / 断头**。

**现状事实**（已确认）：

- 现有笔记**没有双链**，结构完全隐式（只存在于作者脑中）
- 可用结构信号：**文件夹结构 + 文件命名规律**（部分可靠锚点）
- 笔记粒度：**一篇笔记含多个动作**（日记/流水式），笔记 ≠ 节点，需先做事件抽取

**重建闭环**：草稿森林（推断 + 置信度 + 证据）→ Web 确认修正 → 双链写回固化 → 后续运行以已固化结构为骨干。

### 2.2 背景与痛点

找不到/遗忘、知识零散不成体系（树未显式化——正因如此本智能体才有存在价值）、需要汇总产出、仓库太乱想清理。**断头路径检测为核心价值**。

### 2.3 产品形态

- **后端**：Python + FastAPI，本机运行（localhost），直接读写 vault 文件夹，不要求 Obsidian 运行
- **前端**：Vue 3 展示层（森林总览、树详情、确认工作台、写回预览、聊天问答）
- **Obsidian 集成**：本地文件直读直写（唯一数据通道）+ 前端 `obsidian://` URI 跳转原笔记；不使用 Local REST API 插件；不云端部署（路径等配置化预留）

### 2.4 Agent 架构（角色矩阵）

| 环节 | LLM 角色 | 形态 |
|---|---|---|
| 提炼与事件抽取 | 抽取器 | 单次结构化调用，批量并行 |
| 关联判定 | 判断器 | 单次调用 + 依据 + 置信度 |
| 树重建 | 推理器 | **工具型 mini-Agent（ReAct）**：搜候选树 / 回读笔记原文 / 查时间线 / 查事件库 |
| 状态判定 | 判断器 | 单次调用 + 证据 |
| 综述/树页撰写 | 撰写者 | 长上下文单次生成，引用约束 |
| 问答 | 对话 Agent | **多轮 + 会话记忆 + 指令集**（Web 聊天界面） |

**代码职责（不交给 LLM）**：流程编排与断点管理、文件扫描/稳定哈希/增量判断、候选关联生成算法、树数据结构、写回安全机制、任务调度与并发护栏。

**九阶段工作流**：

```
初始化 → 采集 → 提炼/事件抽取 → 关联推断 → 树重建(Agent) → 状态判定
       → 产物生成 → 确认修正(Web) → 双写回(Web 预览确认)
问答服务常驻；增量运行贯穿（变更清单驱动）
```

### 2.5 运行模式

首次全量 + 后续增量；草稿全自动生成 → Web 复核修正 → 确认后写回固化；问答为独立聊天入口。整理运行期间提示用户避免在 Obsidian 同时编辑同批笔记。

### 2.6 约束（均已确认）

- 后端 FastAPI / 前端 Vue 3 / 本机 localhost
- LLM 走 OpenAI 兼容云端 API（DeepSeek 先行，通义千问可配置切换）；**全部笔记内容可上云**
- Chroma 向量库本地持久化；运行状态/会话记忆用本地数据库（SQLite，概要设计定）
- 整理范围 = 整个仓库；排除 `.obsidian/`、附件目录、非 md 文件（含 `.canvas`，留未来）、其他可配置目录
- 修改原笔记仅限**标签回写**与**双链写回**：Web 预览 → 确认 → 只增不删 → 时间戳备份 → 幂等
- 主干代码编排；Agent 仅限树重建与问答两环节
- 全链路中文输出（树页/综述/问答）

### 2.7 假设

- 规模 200–2000 篇
- 完成信号多样（勾选框/状态词/收尾内容），LLM 判定须附证据
- 时间取信分级：frontmatter 日期 > 命名中的日期规律 > 文件时间 > 内容线索，产物留痕

## 三、功能需求

### 整理主链

- **FR-1 笔记采集**：递归扫描整个仓库；解析 frontmatter / 中文与嵌套标签 / 已有链接；提取文件夹路径与文件名规律为结构信号；排除目录与文件类型可配置；笔记标识跨运行稳定（稳定 ID + 内容哈希）
- **FR-2 事件抽取与提炼**：逐篇 LLM 抽取（事件及其时间/状态线索 + 标题确认 + 摘要 + 关键词 + 候选标签，JSON 输出）；内容未变笔记跳过；**单篇失败跳过并标记，输出失败清单，不阻塞全流程**；超长笔记分段摘要再合并
- **FR-3 关联推断**：代码生成候选关联（文件夹/命名规律/向量相似度/时间），LLM 逐组判定，每条关联附依据 + 置信度
- **FR-4 树重建（Agent）**：ReAct 智能体（工具：搜候选树/回读原文/查时间线/查事件库）识别树根、挂接事件，输出草稿森林，逐项置信度标注
- **FR-5 状态判定与断头检测（核心）**：每棵树判定 已完成/进行中/断头（**确认|疑似**），附证据；断头清单为一级输出
- **FR-6 产物生成**（写入 vault `_noteagent/`）：树页（路径结构 + 节点摘要与时间 + 状态及证据 + "来龙去脉"综述段）+ 森林总览；双链格式引用；超大树分层摘要
- **FR-7 增量运行**：变更清单驱动；未变笔记不重复调 LLM；已确认结构与已写回双链作为骨干；中断可续跑

### Web 服务与交互

- **FR-8 服务化**：REST API（触发整理/查询任务/获取树与森林/问答）；整理为**异步长任务**，SSE/WebSocket 推送进度；任务互斥（运行中拒绝重复触发）；状态持久化，服务重启可恢复
- **FR-9 Web 确认工作台**：草稿森林可视化（按状态/置信度筛选），低置信项优先呈现；页面上直接修正（误挂/漏挂/合并/拆分树）并持久化；仅重新生成受影响产物
- **FR-10 双写回（需逐项授权）**：标签回写 + 双链写回：Web 逐篇 diff 预览 → 确认 → 只增不删 → 备份（时间戳目录，保留最近 N 次）→ 幂等
- **FR-11 重置与版本化**：一键清零（向量库/数据库/产物目录）；产物版本化保留最近 N 版，可回退上一版
- **FR-12 前端展示**：森林总览（交互式、状态筛选）、树详情（路径+时间线+证据+综述，`obsidian://` 跳转原笔记）、任务进度、聊天页
- **FR-13 问答入口（Agent）**：多轮对话 + 会话记忆；指令集（清除上下文/重新生成/导出）；基于树结构 + 向量检索，回答可溯源

## 四、非功能需求

- **NFR-1 可靠性**：任何运行（含中断）不损坏/丢失/移动/删除用户原始笔记
- **NFR-2 可恢复性**：断点续跑，操作幂等
- **NFR-3 推断透明性**：草稿森林所有判定附证据与置信度，低置信项优先暴露，Web 可修正
- **NFR-4 编排可控性**：主干代码编排；Agent 有工具白名单/步数上限/超时与成本护栏
- **NFR-5 成本**：全量 1000 篇 API 费用个位数人民币（DeepSeek 价格）
- **NFR-6 性能**：全量 1000 篇 1 小时内（常规网络）；增量只与变更量相关；Web 页面常规响应
- **NFR-7 易用性**：草稿生成零交互；核心流程浏览器完成
- **NFR-8 可维护性**：模型/路径/排除项可配置；遵循 src 分层与 Base* 抽象基类模式；前后端 API 契约清晰
- **NFR-9 安全边界**：服务仅监听本机；vault 写操作仅限双写回通道
- **NFR-10 可测试性**：核心模块（解析/事件抽取校验/树构建/写回安全）配 pytest 自动化测试，其余 Web 手动验收

## 五、接口与数据需求

**外部接口**：

- OpenAI 兼容 LLM API（DeepSeek 先行）
- vault 本地文件系统（读 = 全仓库；写 = 仅双写回通道）
- `obsidian://` URI（前端出站跳转）

**领域概念**（详细模型留待概要设计）：Note（容器）、Event（事件，含时间/状态线索）、Tree（含验证状态）、PathStatus（状态+证据）、Association（依据+置信度）、会话记忆、修正记录、结构清单、任务与进度。

## 六、工程与验收约定

- **开发数据**：样例仓库 20–50 篇（刻意埋入边界案例：烂尾想法、跨文件夹任务、日记式多篇多动作、命名规律文件），开发调试用它；**验收用真实仓库副本**
- **验收要点**：扫描齐全/标识稳定/重跑零调用/失败清单有效；抽验召回可接受/修正生效；已知烂尾入清单/确认疑似区分；写回后重跑沿用结构；异步任务+进度+互斥+重启恢复；diff 与实际一致/备份可恢复；重置干净/版本可回退；`obsidian://` 可打开原笔记；多轮连贯/指令生效/可溯源

## 七、未来方向（不排期）

canvas 白板解析、Docker 容器化、云端部署、领域分组视图、Mermaid 时间线、完整体检报告（矛盾/重复检测）、笔记变动监听、Obsidian 插件形态、移动端

## 八、需求确认记录（13 轮讨论汇总）

- **2026-08-29**：痛点四项（找不到/零散/汇总/清理）；笔记类型=思考记录为主；运行模式=全量+增量；交互=全自动+事后确认；隐私=全部可上云；范围=整个仓库
- **2026-08-31 领域模型**：森林结构；树页+森林总览；断头检测核心；一篇多动作需事件抽取；现有无双链，信号=文件夹+命名规律；确认后写回双链；综述落点=树页叙事段；标签回写保留
- **2026-08-31 Agent 定位**：混合架构（代码编排骨架）；树重建=ReAct Agent；问答=多轮+记忆+指令集；学习优先；编排不交 LLM
- **2026-08-31 产品形态**：本机 Web 服务；FastAPI+Vue3；vault 直读直写+`obsidian://` URI；不用 REST 插件；不云端部署
- **2026-08-31 工程补充**：样例仓库开发+真实副本验收；重置+版本化；非 md 全排除（canvas 留未来）；核心模块 pytest
